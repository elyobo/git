# Installation

```
# Go home
cd ~
# Backup old .gitconfig
mv .gitconfig .gitconfig-old
# Install new config
git clone https://github.com/elyobo/git.git .gitdir 
ln -s ~/.gitdir/gitconfig .gitconfig

# Done!
