[user]
    name = Liam O'Boyle
    email = liam@ontheroad.net.nz

[color]
    diff = auto
    status = auto
    branch = auto
    ui = auto

[core]
	autocrlf = input
	excludesfile = ~/.gitdir/ignore
    editor = /usr/bin/vim
    pager=less -x4
	safecrlf = true

[branch]
	autosetupmerge = true

[push]
	default = tracking

[credential]
	helper = cache --timeout=3600

[diff]
	algorithm = patience

[include]
    path = .gitdir/alias.conf
